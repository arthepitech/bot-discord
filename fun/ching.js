const Discord = require("discord.js")

module.exports = {
  name: "ching",
  description: "racisme pur",
  // category: "Fun",
  async run(funner, message) {
    await message.reply(`Ching Chong Ping Pong !`)
  }
}
